export const categories: string[] = [
    'all', // no category restrictions (name may be changed, meaning may not be changed)
    'art',
    'biography',
    'computers',
    'history',
    'medical',
    'poetry',
  ];

export const sortOptions: string[] = ['relevance', 'newest'];

export const paginationStep = 30; // max = 40